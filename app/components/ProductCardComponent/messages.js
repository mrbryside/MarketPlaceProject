/*
 * ProductCardComponent Messages
 *
 * This contains all the text for the ProductCardComponent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ProductCardComponent';

export default defineMessages({
  addtoCartButtonText: {
    id: `${scope}.addtoCartButtonText`,
    defaultMessage: 'Add to Cart',
  },
});
